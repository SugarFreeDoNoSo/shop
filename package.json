{
  "name": "elegancia-shop",
  "version": "1.0.0",
  "scripts": {
    "dev": "npx @tailwindcss/cli -i ./assets/main.css -o ./assets/output.css --optimize --watch",
    "build": "npx @tailwindcss/cli -i ./assets/main.css -o ./assets/output.css --optimize --minify",
    "test": "playwright test",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.1"
  }
}
